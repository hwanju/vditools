i=0
for pid in ${VM_PID[@]}
do
	/bin/echo $pid > $CPUCTL_ROOT/cgroup.procs
	rmdir ${GROUP_PATH[i]}
	let i=i+1	
done

echo end_time=`date '+%s'` >> /tmp/$mode.schedstat
cat /proc/schedstat >> /tmp/$mode.schedstat
